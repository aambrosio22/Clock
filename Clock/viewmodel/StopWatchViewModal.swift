//
//  StopWatchViewModal.swift
//  Clock
//
//  Created by Antonia Ambrosio on 21/11/22.
//

import Foundation
/*extension StopWatchView {
    
    enum mode{
        case running
        case stopped
        case paused
    }
    
    class ManagerClass: ObservableObject{
        @Published var secondElapsed = 00.0
        @Published var mode: mode = .stopped
        var timerVar = Timer()
        
        public func startTimer(){
            mode = .running
            timerVar = Timer.scheduledTimer(withTimeInterval: 0.1, repeats: true){ timerVar in
                self.secondElapsed += 00.1
            }
        }
        
        public func stopTimer(){
            timerVar.invalidate()
            secondElapsed = 0
            mode = .stopped
        }
        
        public func pauseTimer(){
            timerVar.invalidate()
            mode = .paused
        }
    }
}
*/
